# 🚀 Начни отсюда

## Где что лежит

### Документы разработки интерфейса
- **`docs/INTERFACE_DEVELOPMENT.md`** — подход к разработке (разделение функциональности и дизайна)
- **`docs/INTERFACE_SPEC.md`** — спецификация интерфейса (что должно быть)
- **`docs/START_HERE.md`** — этот файл (быстрый старт)

### Исходный код
- **`src/layouts/`** — HTML шаблоны страниц
- **`src/styles/`** — CSS стили
- **`src/core/`** — функциональные компоненты (будет создано)
- **`scripts/`** — скрипты сборки и импорта

### Данные из core
- **`data/`** — импортированные данные из vovaipetrova-core
  - `routes.json` — маршруты сайта
  - `kb_glossary_lite.jsonl` — база знаний
  - `stories_digests.jsonl` — Stories
  - `tokens.json` — дизайн-токены

### Собранный сайт
- **`dist/`** — готовый сайт для деплоя (генерируется автоматически)

## Что делать при сбое или обновлении сессии

### 1. Прочитай этот файл (`docs/START_HERE.md`)
   - Поймёшь текущее состояние проекта
   - Увидишь структуру

### 2. Посмотри спецификацию (`docs/INTERFACE_SPEC.md`)
   - Что должно быть реализовано
   - Какие компоненты нужны

### 3. Проверь текущий код
   - `src/layouts/index.html` — главная страница
   - `src/styles/main.css` — стили
   - `scripts/build.mjs` — как собирается сайт

### 4. Запусти локально
   ```bash
   npm run import  # Импорт данных из core
   npm run build   # Сборка сайта
   # Открой dist/index.html в браузере
   ```

## Текущее состояние

**Что работает:**
- ✅ Импорт данных из core
- ✅ Базовая сборка сайта
- ✅ GitHub Pages деплой
- ✅ **Классический интерфейс сайта** с меню, разделами и подразделами
- ✅ Независимая структура сайта (не привязана к структуре ядра)

**Структура сайта:**
- Главная (`/`)
- Услуги (`/services`) с подразделами
- Портфолио (`/portfolio`) с подразделами
- База знаний (`/kb`) с подразделами
- О нас (`/about`)
- Блог (`/stories`)

**Где находится структура:**
- `src/config/site-structure.json` — структура сайта (витрины)
- Можно менять независимо от ядра

## Следующий шаг

Наполнить разделы контентом (пока структура готова, контент можно добавить позже).
